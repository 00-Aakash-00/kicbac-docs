---
title: "Install Kicbac SDKs from Verified Source"
sidebarTitle: "Source install"
description: "Install pinned Kicbac JavaScript and Python SDK revisions when a package registry does not yet contain the release."
---

Use the package-manager commands in each SDK guide when the package is
available. If a registry returns `not found`, install the verified source
revision below. Do not substitute an unrelated package with a similar name.

The JavaScript fallback requires Git, Node.js 20.19 or newer, and npm. Each
command invokes the repository's pinned pnpm 9.15.4 version through `npx`, so
it does not depend on a globally installed pnpm shim.

## Python

Install the Python SDK directly from its verified commit:

```bash
python -m pip install "kicbac @ git+https://github.com/00-Aakash-00/kicbac-python.git@8242b8711fcc2663d951702dce84311ae9420d9d#subdirectory=sdk-python"
```

## JavaScript and TypeScript

Clone the verified monorepo revision, build it, and pack the SDKs locally:

```bash
git clone https://github.com/00-Aakash-00/kicbac-js.git kicbac-js
git -C kicbac-js checkout dbbbf4c56317d7e1bf53a1a40bef7adf2605eb03
npx --yes pnpm@9.15.4 --dir kicbac-js install --frozen-lockfile
npx --yes pnpm@9.15.4 --dir kicbac-js build

mkdir -p kicbac-packs
npx --yes pnpm@9.15.4 --dir kicbac-js/packages/kicbac pack --pack-destination "$PWD/kicbac-packs"
npx --yes pnpm@9.15.4 --dir kicbac-js/packages/js pack --pack-destination "$PWD/kicbac-packs"
npx --yes pnpm@9.15.4 --dir kicbac-js/packages/react pack --pack-destination "$PWD/kicbac-packs"
npx --yes pnpm@9.15.4 --dir kicbac-js/packages/nextjs pack --pack-destination "$PWD/kicbac-packs"
npx --yes pnpm@9.15.4 --dir kicbac-js/packages/themes pack --pack-destination "$PWD/kicbac-packs"
```

Install the generated tarballs together so local package dependencies resolve
without reaching the registry:

```bash
npm install ./kicbac-packs/kicbac-0.1.0.tgz \
  ./kicbac-packs/kicbac-js-0.1.0.tgz \
  ./kicbac-packs/kicbac-react-0.1.0.tgz \
  ./kicbac-packs/kicbac-nextjs-0.1.0.tgz \
  ./kicbac-packs/kicbac-themes-0.1.0.tgz
```

Record the pinned commit in your application so upgrades are intentional.
Replace the source fallback with registry versions after those versions are
published.
